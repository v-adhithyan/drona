<?xml version="1.0" encoding="utf-8"?>
  <RelativeLayout
    android:id="@+id/activity_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ceg.avtechlabs.mba.ui.MainActivity">

  <Button
      android:text="Marketing"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_alignParentTop="true"
      android:layout_centerHorizontal="true"
      android:id="@+id/buttonMarketing"
      android:onClick="loadMarketingFeeds"/>

  <Button
      android:text="Finance"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/buttonMarketing"
      android:layout_centerHorizontal="true"
      android:id="@+id/buttonFinance"
      android:onClick="loadFinanceFeeds"/>


  <Button
      android:text="Economics"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/buttonFinance"
      android:layout_alignStart="@+id/buttonFinance"
      android:id="@+id/buttonEconomics"
      android:onClick="loadEconomicsFeeds"/>

  <Button
      android:text="Leadership"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/buttonEconomics"
      android:layout_alignStart="@+id/buttonEconomics"
      android:id="@+id/buttonLeadership"
      android:onClick="loadLeadershipFeeds"/>

  <Button
      android:text="Others"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/buttonLeadership"
      android:id="@+id/buttonOthers"
      android:onClick="loadOtherFeeds"/>
</RelativeLayout>
